from typing import List, Optional

async def extract_ingredients(img_bytes: bytes, filename: Optional[str] = None) -> List[str]:
    """
    Start stubbed so the endpoint works.
    Then replace with real vision model call.
    """
    # TODO: replace with actual model extraction
    return ["egg", "tomato", "garlic"]
